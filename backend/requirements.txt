# WHO AFRO Regional Surveillance Portal — Python backend dependencies
# Install with: pip install -r requirements.txt
# Then pin bcrypt: pip install "bcrypt==4.2.1"

# Web framework & server
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.36
psycopg2==2.9.10

# Auth — JWT + password hashing
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# NOTE: passlib 1.7.4 is incompatible with bcrypt>=5.x
# After installing this file, run: pip install "bcrypt==4.2.1"
bcrypt==4.2.1

# Request parsing & validation
python-multipart==0.0.17
pydantic==2.9.2

# Environment variables
python-dotenv==1.0.1
