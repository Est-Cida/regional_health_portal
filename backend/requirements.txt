# WHO AFRO Regional Health Surveillance Portal — Python backend dependencies
# Install with:  pip install -r requirements.txt
#
# System requirement (not a Python package):
#   PostgreSQL >= 14 with the PostGIS extension enabled
#   Enable in psql: CREATE EXTENSION IF NOT EXISTS postgis;

# ── Web framework & ASGI server ────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36          # ORM + raw SQL execution (ST_AsGeoJSON via text())
psycopg2==2.9.10            # PostgreSQL / PostGIS driver

# ── Authentication — JWT + password hashing ────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# IMPORTANT: passlib 1.7.4 is incompatible with bcrypt >= 5.x
# bcrypt is pinned here to prevent auto-upgrade breaking password verification
bcrypt==4.2.1

# ── Request parsing & validation ───────────────────────────────────────────────
python-multipart==0.0.17    # Required for FastAPI form data (login endpoint)
pydantic==2.9.2             # Request/response schema validation

# ── Environment configuration ──────────────────────────────────────────────────
python-dotenv==1.0.1        # Loads .env file into os.environ at startup
